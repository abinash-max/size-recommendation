# Core FastAPI dependencies
fastapi
mangum>=0.17.0
python-multipart
pydantic

# Google AI (new package)
google-genai
Pillow

# AWS SDK (for S3 access)
boto3

# PyTorch and ML dependencies
# Note: These packages are large. Consider Lambda Layers or Container Images
torch
torchvision
timm
