# Core FastAPI dependencies
fastapi
mangum>=0.17.0
python-multipart
pydantic

# Google AI (new package)
google-genai
Pillow

# AWS SDK (for S3 access)
boto3

# PyTorch and ML dependencies (consider using Lambda Layers for these)
# Note: These packages are large. Consider:
# 1. Using Lambda Layers
# 2. Using container images
# 3. Or removing if classifier is not needed
torch
torchvision
timm

# Note: For Lambda deployment:
# - Package size limit: 250MB (unzipped)
# - Consider Lambda Layers for torch/torchvision/timm
# - Or use AWS Lambda Container Images (10GB limit)
